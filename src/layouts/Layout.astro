---
interface Props {
    title?: string;
}

const { title = "MR Massages Animalier - Bien-être pour Chiens et Chevaux" } =
    Astro.props;
---

<!doctype html>
<html lang="fr">
    <head>
        <meta charset="UTF-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />
        <link rel="icon" type="image/png" sizes="32x32" href="/logo.png" />
        <link rel="icon" type="image/png" sizes="16x16" href="/logo.png" />
        <link rel="apple-touch-icon" sizes="180x180" href="/logo.png" />
        <meta name="generator" content={Astro.generator} />

        <!-- SEO Meta Tags -->
        <meta
            name="description"
            content="MR Massages Animaliers - Morgane RIEHL, masseuse professionnelle pour chiens et chevaux à Boutier-Saint-Trojan (16100). Massages bien-être, sportif et rééducation pour vos animaux en Charente."
        />
        <meta
            name="keywords"
            content="massage animalier, massage chien, massage cheval, Morgane RIEHL, Boutier-Saint-Trojan, Charente, 16100, Seine-et-Marne, 77580, bien-être animal, massage équin, massage canin, ostéopathie animale, kinésithérapie animale, Cognac, Pierre-levée, masseuse animalière, masseur animalier"
        />
        <meta name="author" content="Morgane RIEHL - MR Massages Animaliers" />
        <meta name="robots" content="index, follow" />

        <!-- Open Graph / Facebook -->
        <meta property="og:type" content="website" />
        <meta
            property="og:url"
            content="https://www.mrmassagesanimaliers.com/"
        />
        <meta
            property="og:title"
            content="MR Massages Animaliers - Morgane RIEHL | Massage pour Chiens et Chevaux"
        />
        <meta
            property="og:description"
            content="Massages professionnels pour chiens et chevaux à Boutier-Saint-Trojan. Bien-être et performance pour vos animaux."
        />
        <meta
            property="og:image"
            content="https://www.mrmassagesanimaliers.com/logo.png"
        />

        <!-- Twitter -->
        <meta property="twitter:card" content="summary_large_image" />
        <meta
            property="twitter:url"
            content="https://www.mrmassagesanimaliers.com/"
        />
        <meta
            property="twitter:title"
            content="MR Massages Animaliers - Morgane RIEHL | Massage pour Chiens et Chevaux"
        />
        <meta
            property="twitter:description"
            content="Massages professionnels pour chiens et chevaux à Boutier-Saint-Trojan. Bien-être et performance pour vos animaux."
        />
        <meta
            property="twitter:image"
            content="https://www.mrmassagesanimaliers.com/logo.png"
        />

        <!-- Schema.org Markup -->
        <script type="application/ld+json">
            {
                "@context": "https://schema.org",
                "@type": "LocalBusiness",
                "name": "MR Massages Animaliers",
                "image": "https://www.mr-massages-animaliers.fr/logo.png",
                "description": "Services professionnels de massage pour chiens et chevaux",
                "founder": {
                    "@type": "Person",
                    "name": "Morgane RIEHL"
                },
                "address": {
                    "@type": "PostalAddress",
                    "addressLocality": "Boutier-Saint-Trojan",
                    "postalCode": "16100",
                    "addressCountry": "FR"
                },
                "telephone": "+33687933416",
                "email": "mr_massages.animaliers@yahoo.com",
                "priceRange": "€€",
                "openingHoursSpecification": {
                    "@type": "OpeningHoursSpecification",
                    "dayOfWeek": [
                        "Monday",
                        "Tuesday",
                        "Wednesday",
                        "Thursday",
                        "Friday",
                        "Saturday",
                        "Sunday"
                    ],
                    "opens": "09:00",
                    "closes": "19:00"
                },
                "sameAs": ["https://instagram.com/mr_massages.animaliers"],
                "aggregateRating": {
                    "@type": "AggregateRating",
                    "ratingValue": "5",
                    "reviewCount": "3"
                }
            }
        </script>
        <link rel="preconnect" href="https://fonts.googleapis.com" />
        <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
        <link
            href="https://fonts.googleapis.com/css2?family=Playfair+Display:wght@400;500;600;700&family=Inter:wght@300;400;500;600&display=swap"
            rel="stylesheet"
        />
        <link
            rel="stylesheet"
            href="https://unpkg.com/leaflet@1.9.4/dist/leaflet.css"
            integrity="sha256-p4NxAoJBhIIN+hmNHrzRCf9tD/miZyoHS5obTRR9BMY="
            crossorigin=""
        />
        <title>{title}</title>
    </head>
    <body>
        <a href="#main-content" class="skip-link">Aller au contenu principal</a>
        <slot />
    </body>
</html>

<style is:global>
    @font-face {
        font-family: "Montserrat";
        src: url("/fonts/Montserrat/Montserrat-VariableFont_wght.ttf")
            format("truetype");
        font-weight: 100 900;
        font-style: normal;
        font-display: swap;
    }

    @font-face {
        font-family: "Montserrat";
        src: url("/fonts/Montserrat/Montserrat-Italic-VariableFont_wght.ttf")
            format("truetype");
        font-weight: 100 900;
        font-style: italic;
        font-display: swap;
    }

    :root {
        --primary-color: #785444;
        --primary-light: #8f6b59;
        --primary-dark: #5d3f33;
        --secondary-color: #fffcf4;
        --accent-color: #d4a373;
        --text-dark: #2d2d2d;
        --text-medium: #4a4a4a; /* Assombri pour contraste 4.5:1 (WCAG AA) */
        --text-light: #5f5f5f; /* Assombri pour contraste 3:1 minimum */
        --border-color: #e8e3db;
        --shadow-sm: 0 2px 8px rgba(120, 84, 68, 0.08);
        --shadow-md: 0 4px 16px rgba(120, 84, 68, 0.12);
        --shadow-lg: 0 8px 32px rgba(120, 84, 68, 0.16);
    }

    * {
        margin: 0;
        padding: 0;
        box-sizing: border-box;
    }

    html {
        scroll-behavior: smooth;
        overflow-x: hidden;
    }

    body {
        overflow-x: hidden;
        font-family:
            "Montserrat",
            -apple-system,
            BlinkMacSystemFont,
            "Segoe UI",
            Roboto,
            Oxygen,
            Ubuntu,
            Cantarell,
            sans-serif;
        background-color: var(--secondary-color);
        color: var(--text-dark);
        line-height: 1.7;
        font-weight: 400;
        -webkit-font-smoothing: antialiased;
        -moz-osx-font-smoothing: grayscale;
    }

    h1,
    h2,
    h3,
    h4,
    h5,
    h6 {
        font-family: "Montserrat", sans-serif;
        color: var(--primary-color);
        font-weight: 600;
        line-height: 1.3;
        letter-spacing: -0.02em;
    }

    h1 {
        font-size: clamp(2.5rem, 5vw, 4rem);
        font-weight: 700;
    }

    h2 {
        font-size: clamp(2rem, 4vw, 3rem);
        margin-bottom: 1.5rem;
    }

    h3 {
        font-size: clamp(1.5rem, 3vw, 2rem);
    }

    p {
        color: var(--text-medium);
    }

    a {
        color: var(--primary-color);
        text-decoration: none;
        transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
    }

    a:hover {
        color: var(--primary-light);
    }

    section {
        padding: 6rem 2rem;
        max-width: 1400px;
        margin: 0 auto;
    }

    button {
        cursor: pointer;
        border: none;
        background: var(--primary-color);
        color: var(--secondary-color);
        padding: 1rem 2.5rem;
        border-radius: 2px;
        font-size: 0.95rem;
        font-weight: 500;
        letter-spacing: 0.5px;
        text-transform: uppercase;
        transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
        font-family: "Inter", sans-serif;
    }

    button:hover {
        background: var(--primary-dark);
        transform: translateY(-2px);
        box-shadow: var(--shadow-md);
    }

    /* Focus visible pour l'accessibilité clavier */
    *:focus-visible {
        outline: 3px solid var(--primary-color);
        outline-offset: 2px;
    }

    button:focus-visible,
    a:focus-visible,
    input:focus-visible,
    textarea:focus-visible,
    select:focus-visible {
        outline: 3px solid var(--primary-color);
        outline-offset: 2px;
        box-shadow: 0 0 0 3px rgba(120, 84, 68, 0.2);
    }

    /* Screen reader only - visible uniquement pour les lecteurs d'écran */
    .sr-only {
        position: absolute;
        width: 1px;
        height: 1px;
        padding: 0;
        margin: -1px;
        overflow: hidden;
        clip: rect(0, 0, 0, 0);
        white-space: nowrap;
        border-width: 0;
    }

    /* Skip to content link - visible uniquement au focus clavier */
    .skip-link {
        position: absolute;
        top: -100px;
        left: 50%;
        transform: translateX(-50%);
        background: var(--primary-color);
        color: var(--secondary-color);
        padding: 1rem 2rem;
        text-decoration: none;
        border-radius: 4px;
        font-weight: 600;
        z-index: 10000;
        transition: top 0.3s ease;
        box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2);
    }

    .skip-link:focus {
        top: 20px;
    }

    button:active {
        transform: translateY(0);
    }

    /* Respect de la préférence utilisateur pour les mouvements réduits */
    @media (prefers-reduced-motion: reduce) {
        *,
        *::before,
        *::after {
            animation-duration: 0.01ms !important;
            animation-iteration-count: 1 !important;
            transition-duration: 0.01ms !important;
            scroll-behavior: auto !important;
        }
    }

    input,
    textarea,
    select {
        width: 100%;
        padding: 0.875rem 1.125rem;
        border: 1px solid var(--border-color);
        border-radius: 2px;
        font-size: 0.95rem;
        font-family: "Inter", sans-serif;
        background: white;
        color: var(--text-dark);
        transition: all 0.3s ease;
    }

    input:focus,
    textarea:focus,
    select:focus {
        outline: none;
        border-color: var(--primary-color);
        box-shadow: 0 0 0 3px rgba(120, 84, 68, 0.08);
    }

    @media (max-width: 768px) {
        section {
            padding: 4rem 1.5rem;
        }
    }
</style>
